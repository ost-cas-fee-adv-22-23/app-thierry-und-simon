export const timelineRedirect = {
  redirect: {
    destination: '/timeline',
    permanent: false
  }
}

export const indexRedirect = {
  redirect: {
    destination: '/login',
    permanent: false
  }
}
